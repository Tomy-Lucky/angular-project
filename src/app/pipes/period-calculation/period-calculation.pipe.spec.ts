import { PeriodCalculationPipe } from './period-calculation.pipe';

describe('PeriodCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
